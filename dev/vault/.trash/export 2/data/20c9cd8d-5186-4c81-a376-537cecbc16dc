# Conf
---

port : `$= conf.port` : `INPUT[text(placeholder(port)):port]`

user : `$= conf.user` : `INPUT[text(placeholder(user)):user]`

`BUTTON[btn-reload]`

---

Ir para: [main]

```meta-bind-embed
[[sys/model/btn-reload]]
```
